#include "main.h"
#include <stdio.h>

/**
* mul - multiplies two numbers
* @a: for the parameters of my function
* @b: for the parameters of my function
* Return: Always 0.
*/
int mul(int a, int b)
{

	return (a * b);

	return (0);

}
